# Example material is intentionally untrusted

The public keys in this directory correspond to private keys committed under
`tests/fixtures/`. They make the offline walkthrough reproducible. They are not
operator trust roots and must never be used for a real repository or deployment.

Replace every identity, public key, scope, TTL and subject field from a protected
control plane before evaluating real evidence.
