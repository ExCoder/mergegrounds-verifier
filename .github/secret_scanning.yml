# These deterministic Ed25519 keys are public, compromised test vectors. Keep the
# exclusion exact; production key material is never allowed in this repository.
paths-ignore:
  - "tests/fixtures/*.pem"
